// Server-side user resolution replaced by Firebase Auth + AuthProvider client context.
// See lib/auth-context.tsx (useAuth) for client-side auth and lib/firebase-admin.ts for server routes.
export {}
